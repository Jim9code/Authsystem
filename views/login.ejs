<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authentication system</title>
    <link rel="stylesheet" href="login.css">


</head>
<body>
    <div class="out">
        <div class="middle">
            
            <% if (alert ) { %>
                <% if (color ) { %>
                    <div style="background:<%=color %>; padding: 10px; color: white; border-radius:8px;">
                     <center><span style="font-weight: bolder;"> <%=alert %> </span></center>   
                    </div>
                    <% } %>
            <% } %>
            <br><br>
            

            <div class="inside">
                <center><h1>Login</h1></center>
                <br>
                <form id="loginform" action="/login" method="POST">
                    <label for="email">Email Address</label><br>
                    <input type="email" id="Bemail" name="Bemail" placeholder="Email Address" required>
                    <br><br>
                    <label for="password">Password</label><br>
                    <input type="password" id="Bpassword" name="Bpassword" placeholder="Password" required>
                    <br>
                    <div><span>Don't have an account <a href="/register">Register</a></span></div>
                    <br>
                    <div>
                        <button class="logbtn" type="submit">Log in</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
</body>
</html>